import CreateCourse from '@/components/admin/CreateCourse';
import React from 'react';

const page = () => {
    return (
        <div>
            <CreateCourse />
        </div>
    );
};

export default page;